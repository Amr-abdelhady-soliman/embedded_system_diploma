#include "stdio.h"
void main ()
{
	setvbuf(stdout, NULL, _IONBF, 0);
	setvbuf(stderr, NULL, _IONBF, 0);
	int n;
	int x[30];
	printf("enter number of elements of the array \n");
	scanf("%d",&n);
	printf("enter elements of matrix \n");
	for (i=0;i<n;i++)
	{
			printf("enter elemennt a(%d) \n",i+1);
			scanf("%d",&x[i]);
	}
	printf("display elements of matrix \n");
	for (i=0;i<r;i++)
	{
			printf("%d \t",x[i]);
	}
	printf ("\n enter the number ou want to insert  : \n");
	for (i=0;i<c;i++)
	{
		for (j=0;j<r;j++)
		{
			y[i][j]=x[j][i];
			printf(" %d \t",y[i][j]);
		}
		printf("\n");
	}

}
